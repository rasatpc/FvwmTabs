# FvwmTabs v12 FVWM functions
# FVWMTABS_DIR is set by the FvwmTabs module from its actual ModulePath.
SetEnv FVWMTABS_DEFAULT_ID 1

DestroyFunc FvwmTabsStart
AddToFunc FvwmTabsStart
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" start

DestroyFunc NewTabber
AddToFunc NewTabber
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" createNewTabber $*

DestroyFunc FvwmTabs_AddPicked
AddToFunc FvwmTabs_AddPicked
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" add $0 "$[w.id]" "$[w.name]"

DestroyFunc FvwmTabs_TabizeLoop
AddToFunc FvwmTabs_TabizeLoop
+ I Pick Function FvwmTabs_AddPicked $0
+ I TestRc (NoMatch) Break
+ I Function FvwmTabs_TabizeLoop $0

# Legacy/default tabber commands. These stay for existing key bindings and
# FvwmConsole habits; the root menu below uses the active-tabber variants.
DestroyFunc Tabize
AddToFunc Tabize
+ I Function FvwmTabs_TabizeLoop $[FVWMTABS_DEFAULT_ID]

DestroyFunc TabizeActive
AddToFunc TabizeActive
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" tabizeActive

DestroyFunc TabizeTo
AddToFunc TabizeTo
+ I Function FvwmTabs_TabizeLoop $0

DestroyFunc DestroyTabber
AddToFunc DestroyTabber
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" destroyTabber $[FVWMTABS_DEFAULT_ID]

DestroyFunc DestroyActiveTabber
AddToFunc DestroyActiveTabber
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" destroyActiveTabber

DestroyFunc DestroyTabberId
AddToFunc DestroyTabberId
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" destroyTabber $0

DestroyFunc NextTab
AddToFunc NextTab
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" nextTab $[FVWMTABS_DEFAULT_ID]

DestroyFunc NextTabActive
AddToFunc NextTabActive
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" nextTabActive

DestroyFunc NextTabId
AddToFunc NextTabId
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" nextTab $0

DestroyFunc PrevTab
AddToFunc PrevTab
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" prevTab $[FVWMTABS_DEFAULT_ID]

DestroyFunc PrevTabActive
AddToFunc PrevTabActive
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" prevTabActive

DestroyFunc PrevTabId
AddToFunc PrevTabId
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" prevTab $0

DestroyFunc QuitFvwmTabs
AddToFunc QuitFvwmTabs
+ I Exec exec python3 "$[FVWMTABS_DIR]/tabber_client.py" quit

# Root menu actions target the active tabber, falling back to tabber 1 inside
# the Python server when no active tabber is known.
DestroyMenu TabbersMenu
AddToMenu TabbersMenu "FvwmTabs" Title
+ "Create New Tabber" Function NewTabber
+ "Add Window(s)" Function TabizeActive
+ "" Nop
+ "Next Tab" Function NextTabActive
+ "Previous Tab" Function PrevTabActive
+ "" Nop
+ "Destroy Tabber" Function DestroyActiveTabber

# Optional key bindings (users may comment out)
Key T A CM Function NewTabber
Key W A CM Function Tabize
Key Right A CM Function NextTab
Key Left A CM Function PrevTab

# Optional root-window menu binding (comment out if you already use right-click root menus)
Mouse 3 R A Menu TabbersMenu mouse 0p 0p
